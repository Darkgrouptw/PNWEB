<link href="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.3/css/select2.min.css" rel="stylesheet" />
<script src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.3/js/select2.min.js"></script>

<div class="BigTitle">
	<h2>媒體控管頁面</h2>
	<hr>
</div>
<div class="BigTitle">
	<h3>媒體合併功能</h3>
	<h4>將媒體合併，會將後者的意見全部導向至前者（後者不會刪除），此步驟無法復原請小心使用。</h4>
</div>
<form action="<%=mergeMedia_path%>" method="post" align="center">
	令
	<select id="browsers" name = p1 class="js-example-basic-single">
		<% @allMedia.each do |media| %>
			<option value="<%= media.name%>">
				<%= media.name %>
			</option>
		<% end %>
	</select>
	<input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
	併吞
	<select id="browsers" name = p2 class="js-example-basic-single">
		<% @allMedia.each do |media| %>
			<option value="<%= media.name%>">
				<%= media.name %>
			</option>
		<% end %>
	</select>
	<input type="submit" value="確定合併(無法復原)">
</form>

<div class="BigTitle">
	<h3>媒體更名功能</h3>
	<h4>將媒體更名，此步驟無法復原請小心使用。</h4>
</div>
<form action="<%= changeMediaName_path %>" method="post" align="center">
	令
	<select id="browsers" name = p1 class="js-example-basic-single">
		<% @allMedia.each do |media| %>
			<option value="<%= media.name%>">
				<%= media.name %>
			</option>
		<% end %>
	</select>
	<input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
	更名為
	<input type="text" name= p2 maxlength="30"/>
	<input type="submit" value="確定更名(無法復原)">
</form>

<div class="BigTitle">
	<h3>媒體影藏功能</h3>
	<h4>將媒體隱藏，使之不會出現在首頁的排行版上。</h4>
</div>
<form action="<%= media_hide_path %>" method="post" align="center">
	令
	<select id="browsers" name = Name class="js-example-basic-single">
		<% @allMedia.each do |media| %>
			<% if getOtherParameter(media,"hide") != "yes" %>
				<option value="<%= media.id %>">
					<%= media.name %>
				</option>
			<% end %>
		<% end %>
	</select>
	<input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
	<input type="submit" value="隱藏">
</form>
<form action="<%= media_hide_path %>" method="post" align="center">
	令
	<select id="browsers" name = Name class="js-example-basic-single">
		<% @allMedia.each do |media| %>
			<% if getOtherParameter(media,"hide") == "yes" %>
				<option value="<%= media.id %>">
					<%= media.name %>
				</option>
			<% end %>
		<% end %>
	</select>
	<input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
	<input type="submit" value="顯示">
</form>
<hr>
<script type="text/javascript">
	$('.js-example-basic-single').select2();
</script>