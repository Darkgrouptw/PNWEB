<% if current_user.nil? %>
	error please verify you self
<% else %>
	<div class >
		<div class = "container">
			<div class = "jumbotron">
				<h2 style="text-align:center;">新增議題</h2>
				<div class = "row">
					<from action= "<%= issuelist_new_path %> " method= "post">
						<label>Title*</label>
                        <input type="text" class="form-control" name="title" required="true" >
                        <br>
                        <label>description*</label>
                        <textarea class="form-control" name="post" cols = "50" rows="5">
                        </textarea>
                        <br>
                        <label>trunkID (if no trunk don't fill data)</label>
                        <input type="text" class="form-control" name="trunk_id" >
                        <br>
                        <label>tag (separate with ',')</label>
                        <input type="text" class="form-control" name="tag" >
                        <br>
					</form>
				</div>
			</div>
		</div>
	</div>
<% end %>
